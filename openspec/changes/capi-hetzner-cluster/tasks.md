# Tasks

Each task names its verification.
Stage numbers S3, S4, S4b, S5 match proposal.md and ADR-007's revision-2 stage table; tasks within a stage depend on the preceding tasks of that stage unless stated.
S3 depends on `openspec/changes/k3s-nixos-vm-tests/tasks.md` S2 having passed (`vm-k3s-platform` green on the KVM developer host).
S4 depends on S3 and on the spend gate of design D10: Hetzner spend is approved in principle, and each flake revision that bills is released by `VANIXIETS_HETZNER_SPEND_APPROVED=<flake rev>` in the operator's shell; no effect calls the Hetzner Cloud API without it, and a green S3 releases nothing.
Every S4 task that bills carries the marker `[bills Hetzner]` and a cost line naming the billed resources; the amounts are Hetzner list prices at execution time and are recorded in the task's PR body (charter open question Q7).
S4b depends on S4 and bills nothing new (the gateway runs on M5).
S5 is deferred, GCP-only (design D11), and has no scheduled task.
Design Q1–Q7 are resolved (design.md Open Questions); no ambiguity gates a task here.
No task edits `kubernetes/clusters/local*`, `kubernetes/nixidy/`, `kubernetes/tests/local-k3d/`, `modules/apps/cluster/`, `.github/workflows/`, or the flake inputs, and every task's verification includes a `git diff --stat` check that those paths are absent.
Every effect and long-running command is captured with `2>&1 | tee logs/<name>-$(date +%Y%m%d-%H%M%S).log`.

## D. Design (this change)

- [x] D.1 Split S3–S5 out of `k3s-nixos-vm-tests` into this change with brainstorm, proposal, design, tasks, and the delta specs `capi-cluster-rendering`, `capi-bootstrap-vm-regulator`, `capi-management-cluster`, `hetzner-cluster-deployment`, `k8s-admin-overlay`, `world-assumptions` (A17–A21) — verify: `direnv exec . openspec validate --all` passes; every requirement cites its ADR decision and coverage bin; the parent change's artifacts reference this change by path and contain no S3–S5 implementation task

## S3. Management handler, provider rendering, CAPI render, and the bootstrap leaf (KVM for S3.6–S3.8 only)

- [ ] S3.1 Add `modules/kubernetes/capi/providers.nix`: vendored release manifests of Cluster API core, CAPH, and cluster-api-k3s at pinned versions as fixed-output derivations, a rendered `clusterctl.yaml` whose `providers[]` `url`s are their store paths, and `checks.k8s-capi-providers` asserting exactly three providers, each with a version and a store-path `url`, and every image reference in the manifests digest-pinned — verify: `nix build .#checks.x86_64-linux.k8s-capi-providers` passes; replacing one image digest with a tag makes it fail naming the image; removing the `k3s` bootstrap entry makes it fail; revert
- [ ] S3.2 Add `kubernetes/modules/capi/` (cloud-invariant core: `Cluster`, `KThreesControlPlane` replicas 1, `KThreesConfigTemplate`, `MachineDeployment` replicas 1, `MachineHealthCheck`) and `kubernetes/modules/platform/{default,hetzner,aws,kubevirt}.nix` (`platform` sum; `hetzner` renders `HetznerCluster` and two `HCloudMachineTemplate`s with `imageName` from the per-role label; `aws` and `kubevirt` throw `platform.<variant> is declared but not implemented`; unknown provider is a module-system type error), and `platform = hetzner` in `kubernetes/clusters/cryolite/topology.nix` — verify: `nix eval` of the rendered tree lists the nine objects; setting `platform = kubevirt` or `platform = aws` fails with the distinct message naming the variant; setting `platform = "azure"` fails with the type error; revert
- [ ] S3.3 Render every T0 item as `files[]` entries with `contentFrom.secret{name: "cryolite-<item>", key}` (token and CA drop-ins under `/etc/rancher/k3s/config.yaml.d/`, etcd-S3 credentials, Flux age-key `Secret` manifest under `/var/lib/rancher/k3s/server/manifests/` on the server; SSH CA and cosign material per design D3), `agentConfig.airGapped: true`, and the provider's fixture `runcmd`; extend `checks.k8s-node-identity-free-cryolite` to take the rendered `capi-cryolite` tree as an input — verify: the regulator passes; adding an inline `content` for the token makes a new assertion fail naming the file; adding `nodeName: cryolite-server-0` makes the identity regulator fail; revert
- [ ] S3.4 Add `modules/checks/k8s-capi-render.nix` → `checks.k8s-capi-render-cryolite` diffing the render against per-object goldens under `kubernetes/tests/cryolite/golden/capi/` (design D2) and asserting each `imageName` equals the label the S4.1 image derivation computes — verify: passes; changing `MachineDeployment.spec.replicas` to 2 makes it fail naming the object; changing the label function makes it fail on `imageName`; revert
- [ ] S3.5 Add `modules/kubernetes/capi/management.nix` and `modules/apps/k8s/{mgmt-k3d,clusterctl-init,clusterctl-move}` (Handler B, design D1; `apps.k8s.mgmt-k3d`): create the k3d cluster on Colima, `clusterctl init --config <store>/clusterctl.yaml` with the three providers, apply the rendered tree and T0 `Secret`s, print the handler contract's observations (kubeconfig path, provider `Ready` conditions, applied object names), and read the spend gate before any step that would reach the Hetzner Cloud API; add clusterctl and hcloud to `modules/devshells/kubernetes.nix` — verify: the transcript on stibnite shows all three providers `Ready` and the `Cluster` object accepted with `InfrastructureReady=False` awaiting Hetzner credentials (no spend); the `cryolite-server` bootstrap `Secret` the provider generates contains the `contentFrom.secret` items resolved and no inline T0 value the repository holds; `git diff --stat` shows the devshell file gaining lines only
- [ ] S3.5b Add the shared gate function under `modules/apps/k8s/` and `checks.k8s-spend-gate-cryolite` (design D10, KVM-free): every Hetzner-calling effect's dry-run entry point is run with `VANIXIETS_HETZNER_SPEND_APPROVED` unset and with a wrong revision and must exit non-zero naming the expected revision before any API call; with the matching revision the dry-run plan must list exactly the expected Hetzner actions — verify: `nix build .#checks.x86_64-linux.k8s-spend-gate-cryolite` passes; editing one effect to skip the gate read makes it fail naming the effect; revert
- [ ] S3.6 Add `modules/nixos/k3s-server/capi-bootstrap.nix`: `k3s-server.bootstrap = "clan-vars" | "cloud-init"` (default `clan-vars`, so existing consumers are unchanged), `cloud-init` enabling `services.cloud-init` with the NoCloud datasource only, the `/opt/install.sh` shim, and `k3s.service` ordered after `cloud-final.service`; import it from `default.nix` — verify: `nix eval` of `cryolite-server` with `bootstrap = "cloud-init"` shows the shim and the ordering; with the default, the evaluated configuration is byte-identical to before this task
- [ ] S3.7 Add `modules/checks/vm-k3s-capi-bootstrap.nix`: `cryolite-server` image with `bootstrap = "cloud-init"`, a seed ISO whose `user-data` is the S3.3 template with `contentFrom.secret` resolved against `kubernetes/tests/fixtures/` (parent S2.2), `memorySize = 1536`, `diskSize = 4096` — verify: `nix build .#checks.x86_64-linux.vm-k3s-capi-bootstrap 2>&1 | tee logs/vm-k3s-capi-bootstrap-$(date +%Y%m%d-%H%M%S).log` passes on the KVM host
- [ ] S3.8 Write the bootstrap test script: `cloud-final.service` completed, `/opt/install.sh` ran with `INSTALL_K3S_SKIP_DOWNLOAD=true`, `k3s.service` active with `ExecStart` reading `services.k3s.configPath`, the merged config contains the token and CA paths from `config.yaml.d/`, the Flux age-key `Secret` manifest is present under `server/manifests/` and applied, the host key is signed by the fixture SSH CA, a second boot from a fresh disk yields a different host key also valid under the CA, and the CA private key is absent from the disk after `k3s.service` is active — verify: the leaf passes; the assertions are listed in the PR body against ADR-010 R10.3, R10.4, R10.e
- [ ] S3.9 Record mutation evidence: remove the `runcmd` from the seed, capture `k3s.service` inactive; replace the token drop-in with an inline value, capture the S3.3 assertion; sign the host key with a foreign CA, capture the CA assertion; revert all — verify: the three failure outputs and the revert are in the PR body and `git diff main -- modules/nixos/k3s-server/` shows only the additive files

## S4. First Hetzner deployment (effectful; bills Hetzner; released per flake revision)

Billing surface of this stage (design D10): S4.1–S4.2 snapshot publication (M4 server-hours while the image is written, snapshot storage per revision per role, two roles, two revisions retained); S4.3 CAPH creates one load balancer and two servers (M5, M6) that run until deleted; S4.0/S4.7 the etcd-S3 bucket (storage and requests, fleet-level, outlives the cluster); S4.6 and every later node cycle or rebuild repeat S4.2 and replace a server.
Each task's cost line names the billed resources; list prices are looked up at execution time and recorded in the PR body.

- [ ] S4.0 GATE and prerequisites: the operator has set `VANIXIETS_HETZNER_SPEND_APPROVED` to the flake revision this stage deploys and the PR description records that revision; the L0 review item confirms the complete prerequisite set is the repository, `secrets/clusters/cryolite/`, one Hetzner API token, and stibnite (ADR-010 R10.5); the etcd-S3 bucket is declared in the fleet Terranix (design D8) through the `hcloud` provider if it exposes a bucket resource and otherwise through the `aws` or `minio` provider against Hetzner's S3 endpoint with a console-created access key entered into T0 — `[bills Hetzner]` cost: one Object Storage bucket (storage and requests) from this point on — verify: the PR body records the released revision, the review item, and which Terraform provider declared the bucket (ADR-009 R9.j updated); `k8s-spend-gate-cryolite` passes; no `hcloud` call precedes the gate
- [ ] S4.1 Add `modules/kubernetes/hetzner/image.nix` (raw disk images of `cryolite-server` and `cryolite-agent` from the fleet disko layout, built on the Linux builder M2; one pure image derivation, design D5) and `hetzner/snapshot.nix` with the label function `caph-image-name=cryolite-<role>-<closure-hash-prefix>` — bills nothing (build only) — verify: `nix build .#packages.x86_64-linux.cryolite-server-image` on M2 produces the image; the label equals the one S3.4 asserted in the CRs
- [ ] S4.2 Add `modules/apps/k8s/hetzner-snapshot-publish`: read the spend gate, create M4 in rescue mode, write the image, snapshot with the label, delete M4, exit 0 without acting when the label exists, prune older labels for the role beyond the newest two (design D5), refuse a label whose closure is not in the current evaluation — `[bills Hetzner]` cost: M4 server-hours per run (minutes, rounded up to the hour by Hetzner) and snapshot storage per GB-month for two roles times two retained revisions — verify: two consecutive runs on stibnite produce one snapshot per role and the second run's transcript shows no server created; a run with the variable unset exits non-zero before any API call; `hcloud server list` shows no M4 afterwards; the transcripts are in the PR body
- [ ] S4.3 Generate the T0 set with `clan vars generate` for `cryolite`, run Handler B (S3.5) with the Hetzner token `Secret`, apply the T0 `Secret`s and the rendered `capi-cryolite` tree — `[bills Hetzner]` cost: one load balancer and two servers (M5, M6) at hourly rates until deleted, plus the private network (no charge) and public IPv4 addresses — verify: `clusterctl describe cluster cryolite` shows the load balancer, one `KThreesControlPlane` machine (M5), and one `MachineDeployment` machine (M6) `Running`; `kubectl --kubeconfig cryolite.kubeconfig get nodes` shows both `Ready`; the transcript is in the PR body
- [ ] S4.4 Verify T0 delivery and identity on the live nodes: the fingerprint of `/var/lib/rancher/k3s/server/tls/server-ca.crt` on M5 equals the Clan vars value (ADR-010 R10.6); `ssh-keyscan` of M5 and M6 shows host certificates under the T0 SSH CA; the Flux root `Kustomization` is `Ready=True` at the digest in the closure; no `contentFrom.secret` value appears inline in any object of the management cluster — verify: command outputs in the PR body with the fingerprints
- [ ] S4.5 `clusterctl move --to-kubeconfig cryolite.kubeconfig`, then delete M3 — verify: `clusterctl describe cluster cryolite` against `cryolite` shows the same objects and the T0 `Secret`s present (ADR-010 R10.f); `k3d cluster list` on stibnite is empty; the transcript is in the PR body
- [ ] S4.6 Roll one node by bumping the flake revision, releasing the new revision through the gate, republishing the snapshots (S4.2), and applying the re-rendered tree — `[bills Hetzner]` cost: one more S4.2 run and one replacement server's hours overlapping the old one during rollout — verify: CAPH replaces M6 with a new `Machine` whose name, host key, and `network.cilium.io/wg-pub-key` differ from the old one; the old node's objects are gone; `k8s-node-identity-free-cryolite` still passes on the repository
- [ ] S4.7 Enable `--etcd-s3` snapshots with the T0 credentials to the S4.0 bucket — `[bills Hetzner]` cost: snapshot objects and requests in the bucket — verify: one snapshot listed in the bucket; the DR rehearsal (design D8) is recorded as a separately approved follow-up, not performed here
- [ ] S4.8 Update `docs/notes/development/kubernetes/` (README, `workflows/`, a `components/` page for `cryolite`, the charter's billing section with the recorded prices) with the runbook as executed and the M1–M6 table — bills nothing — verify: `prek run --all-files` passes; the pages name the load-balancer endpoint and the SSH CA as the administrative path and state that no overlay exists yet

## S4b. Administrative overlay (effectful)

- [ ] S4b.1 Read `clanServices/wireguard` at the pinned clan-core revision and settle design Q3 (non-machine peer or gateway-as-Clan-machine exception) — verify: the decision and the README lines it rests on are recorded in this stage's PR body and ADR-009 R9.g is updated
- [ ] S4b.2 Add a `wireguard` instance to the Clan inventory with the existing primary VPS as controller and stibnite as an admin-only peer; add the `cryolite` gateway peer key to the T0 generators under `secrets/clusters/cryolite/` — verify: `clan vars generate` produces the key; `k8s-node-identity-free-cryolite` passes (the gateway is per cluster, not per node); the existing ZeroTier configuration is byte-identical (`git diff --stat` on the inventory shows only the new instance)
- [ ] S4b.3 Add the gateway workload to `kubernetes/clusters/cryolite/`: one-replica `hostNetwork` `Deployment` on the control-plane node, peer private key from the T0 `Secret`, allowed subnet the Hetzner private network — verify: `k8s-purity-cryolite` and `k8s-capi-render-cryolite` pass; the rendered tree names no node
- [ ] S4b.4 Deploy through Flux (a flake bump), connect from stibnite over the overlay — verify: `kubectl` against the API server's private address succeeds over the overlay; `ssh` to M5's private address succeeds under the SSH CA; the public-IP path still works; the transcript is in the PR body
- [ ] S4b.5 Record mutation evidence: scale the gateway to 0, capture the overlay path failing while the public path works; restore — verify: both transcripts in the PR body

## S5. Render-only cloud variants and cross-variant equivalence (KVM-free; deferred)

No task is scheduled.
When scheduled, S5 adds `kubernetes/modules/platform/gcp.nix` as a render-only variant with its per-object goldens, `checks.k8s-capi-core-equivalence-cryolite`, `checks.k8s-clustermesh-preconditions`, and the dataplane allowlist derivation, per the `[S5, deferred]` requirements in `capi-cluster-rendering`; `platform.aws` stays a throwing variant (design D11); scheduling S5 is a separate decision recorded in a later revision of this change.

## Integration Verification

- [ ] IV.1 Every requirement in the six delta specs is discharged by a named leaf, effect, review rule, or `[S5, deferred]` marker with a coverage bin and non-vacuity evidence, and every world assumption is cited by the requirements that rest on it — verify: `direnv exec . openspec validate capi-hetzner-cluster` passes and a traceability pass over the specs is in the final PR body
- [ ] IV.2 No `checks.<system>.*` derivation references an `apps` effect — verify: `nix path-info -r` over the checks closure contains none of the `modules/apps/k8s/*` store paths
- [ ] IV.3 The frozen prototypes, the workflow, the flake inputs, the existing Clan machines, and the ZeroTier network are untouched — verify: `git diff main --stat -- kubernetes/clusters/local kubernetes/clusters/local-k3d kubernetes/nixidy kubernetes/tests/local-k3d modules/apps/cluster .github/workflows flake.lock` is empty and the inventory diff shows only the S4b instance
- [ ] IV.4 The L0 root set is the complete prerequisite: a fresh checkout on stibnite with `secrets/clusters/cryolite/` decryptable and one Hetzner token reaches S4.5 with no other machine touched by hand — verify: the S4 runbook transcript lists every host contacted and each is one of M1–M6 or the load balancer
- [ ] IV.5 No k3s node is a Clan machine and none joins ZeroTier, Yggdrasil, or the overlay directly — verify: `clan machines list` contains no `cryolite-*` entry; `zerotier-cli listpeers` on the controller shows no node; the S4b gateway is the only cluster-side peer
