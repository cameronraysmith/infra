## 1. Forge application

- [x] 1.1 Register a dedicated GitHub App for nixbot with repository permissions Contents read, Checks write, Metadata read, Pull requests read, organization permission Members read, and subscribed events push, pull request, check run, and check suite; set its webhook URL to `https://nixbot.scientistexperience.net/webhooks/github` and its user-authorization callback to `https://nixbot.scientistexperience.net/auth/github/callback` — verify: the application's settings page lists exactly those permissions and events, and its numeric id and OAuth client id are recorded in this change's verify.md. Registered as `sciexp-nixbot`, app id `4743700`, OAuth client id `Iv23lie8GaDpY0cPXGg4`, owner `sciexp` (Organization), public; recorded in verify.md §9.1. Permissions and events confirmed an exact match through `gh api /apps/sciexp-nixbot`: `checks:write`, `contents:read`, `members:read`, `metadata:read`, `pull_requests:read`, and events `check_run`, `check_suite`, `pull_request`, `push`. Webhook URL confirmed on the settings page by the operator; the user-authorization callback is confirmed functionally by a completed login round trip (verify.md §9.4).
- [x] 1.2 Install that application with a repository selection covering only repositories intended for nixbot, and record the resulting selection — verify: the application's installations page shows that selection, recorded verbatim in verify.md, and the resolution of the design's open question about whether an empty selection is permitted is recorded with it. Selection recorded verbatim in verify.md §9.2: `cameronraysmith/vanixiets ALONE`; installation id `157111096`, confirmed from the service's own startup log. The open question's resolution is recorded as `OPEN and UNTESTED` in verify.md §9.3: one repository was selected, not none, so an empty selection was never exercised and neither answer is inferred.
- [x] 1.3 Open the incumbent buildbot application's settings and confirm its permissions, events, webhook URL, and installation selection are unchanged — verify: the recorded incumbent settings match the pre-change state, with the application id noted in verify.md. Incumbent `sciexp-buildbot`, app id `3305657`, noted in verify.md §9.5; its settings match the baseline the operator captured before registration — owner `sciexp` (Organization), `events=[]` with nothing subscribed, permissions `contents:read`, `emails:read`, `members:read`, `metadata:read`, `repository_hooks:write`, `statuses:write` — confirmed independently through `gh api /apps/sciexp-buildbot`.

## 2. Flake input

- [x] 2.1 Add the `nixbot` flake input to `flake.nix`, following this repository's `nixpkgs`, and lock it — verify: `nix flake metadata --json` reports a `nixbot` node pinned to a specific revision, and `nix eval .#modules.nixos --apply builtins.attrNames` still evaluates
- [x] 2.2 Confirm the existing `buildbot-nix` input is untouched — verify: `git diff flake.nix` shows only the added `nixbot` input, and `nix flake metadata --json` reports the `buildbot-nix` node at its previous revision

## 3. Credentials

- [x] 3.1 Declare three `clan.core.vars` generators in the new aspect — an operator-populated application private key, a generated webhook secret, and an operator-populated interface-login secret — each owned by the service user with the service named in its restart list — verify: `nix eval .#nixosConfigurations.magnetite.config.clan.core.vars.generators --apply builtins.attrNames` lists the three new generators alongside the incumbent's six
- [x] 3.2 Generate the webhook secret and populate the two operator slots with the values from task 1.1 — verify: `clan vars list magnetite` reports all three as set, and `vars/per-machine/magnetite/` carries one encrypted entry per generator file. All three are set and committed: `31e205454` (application private key, operator-populated), `63d82abcb` (OAuth client secret, operator-populated), `53962942d` (webhook secret, generated by the deployment itself). `vars/per-machine/magnetite/` carries 36 generator entries, three of them nixbot's, one encrypted file each — `nixbot-github-app-secret-key/key.pem/secret`, `nixbot-github-oauth-secret/secret/secret`, `nixbot-github-webhook-secret/secret/secret`. The two operator-populated values are additionally confirmed correct in use: the OAuth secret by a completed login round trip and the private key by the service minting an installation access token at startup (verify.md §9.4, §11).
- [x] 3.3 Confirm no credential value entered the repository — verify: `just lint` passes, including its secret scan, and reading each new `vars/` entry shows an encrypted blob rather than a value. Verified as written; the earlier report that no secret-scanning hook sits behind `just lint` was mistaken and is corrected in verify.md §11. `.pre-commit-config.yaml` declares exactly two hooks and the first is `gitleaks` 8.30.1; `nix develop --command just lint` reports `gitleaks Passed` and `treefmt Passed`, exit 0. The dev shell is required — a bare `just lint` fails with exit 127 because `prek` is not on the ambient PATH. Each of the three new `vars/` entries reads as sops ciphertext (`ENC[AES256_GCM,...]`) with no plaintext key header, and the only files in the tree carrying a `BEGIN ... PRIVATE KEY` line are two documentation guides showing the header form.

## 4. First-party aspect

- [x] 4.1 Write `modules/nixos/nixbot.nix` defining `flake.modules.nixos.nixbot`, configuring the service with its own hostname, its GitHub integration referencing the three credential paths, the topic-based repository adoption disabled, an explicit repository allowlist, provider-qualified administrators, the host's own platform as the only build system, explicitly chosen evaluation worker count and per-worker memory, the local database path, and the module's proxy integration left enabled with certificate issuance requested — verify: `nix eval .#nixosConfigurations.magnetite.config.services.nixbot` sub-attributes return those values, specifically `domain`, `github.topic` as null, `github.repoAllowlist`, `buildSystems`, `evalWorkerCount`, `database.createLocally`, `nginx.enable`, and `nginx.enableACME`
- [x] 4.2 Confirm the verdict namespace is left at the module's default rather than set to the incumbent's — verify: `nix eval .#nixosConfigurations.magnetite.config.services.nixbot.statusContextPrefix` returns the module default and not the incumbent's prefix
- [x] 4.3 Give the aspect file a header documenting its generators and its coexistence constraints, following the incumbent aspect's header form — verify: the file's header names each generator and its consumer, and `just lint` passes

## 5. Host composition

- [x] 5.1 Import the upstream `inputs.nixbot.nixosModules.nixbot` module in `modules/machines/nixos/magnetite/default.nix` alongside the existing upstream imports, and name `nixbot` in that host's aspect list — verify: `nix eval .#nixosConfigurations.magnetite.config.services.nixbot.enable` returns true
- [x] 5.2 Confirm the incumbent's composition is unchanged — verify: `git diff --stat` reports no change under `modules/nixos/buildbot.nix` or the repository-root `buildbot-nix.toml`, and `nix eval .#nixosConfigurations.magnetite.config.services.buildbot-nix.master.domain` still returns the incumbent's hostname
- [x] 5.3 Confirm the two services claim disjoint host resources in the evaluated configuration — verify: `nix eval` of the magnetite configuration shows distinct unit names, distinct users and groups, distinct state directories, and distinct root-retention directories for the two services, and that no TCP port is bound on the new service's behalf

## 6. Hostname

- [x] 6.1 Add an unproxied `nixbot` CNAME to `modules/terranix/cloudflare.nix` following the existing incumbent record — verify: the repository's terraform plan recipe reports exactly one record to add and none to change or destroy
- [x] 6.2 Apply the plan and confirm public resolution — verify: `dig +short nixbot.scientistexperience.net` resolves through to the host's address, and the record is not intercepted by the provider's proxy. `dig +noall +answer nixbot.scientistexperience.net` returns `nixbot.scientistexperience.net. CNAME magnetite.scientistexperience.net.` then `magnetite.scientistexperience.net. A 49.12.12.74`, identical resolution to the incumbent's `dig +short buildbot.scientistexperience.net`. `49.12.12.74` is the Hetzner host's own address rather than a provider anycast address, so the record is unproxied. Recorded in verify.md §11.

## 7. Build gate

- [x] 7.1 Instantiate the host's configuration without deploying it — verify: `nix eval .#checks.x86_64-linux.nixos-magnetite.drvPath 2>&1 | tee logs/nixos-magnetite-eval-$(date +%Y%m%d-%H%M%S).log` succeeds, walking the whole host configuration and catching every evaluation error, option collision and assertion. Amended from the realizing `nix build`, and realization is deferred to deployment on the host: the operator's machine is aarch64-darwin against an x86_64-linux target, so realizing copies the entire system closure locally for information instantiation already supplies. The realizing build was attempted twice and the second attempt failed environmentally — `error: Nix daemon disconnected unexpectedly` on the remote builder while building a home-manager zsh dotfile, cascading through the home-manager generation, alongside HTTP/2 substituter failures — which is unrelated to this change and is not evidence about it. No realization of this host's toplevel has happened.

## 8. Deployment

- [x] 8.1 Deploy the host — verify: `clan machines update magnetite 2>&1 | tee logs/clan-update-magnetite-$(date +%Y%m%d-%H%M%S).log` completes, and on the host `systemctl is-active nixbot.service` reports active with its socket unit present. The deployment ran under the operator's own identity, installing all three credential files. The state it produced is confirmed independently: `nixbot.service` is `active running` (MainPID 1195299, started Aug 28 02:01:03) and `nixbot.socket` is `active running`, alongside `acme-nixbot.scientistexperience.net.service` and its renewal timer. Corroborating 5.3 at runtime, uvicorn listens on `/run/nixbot/web.sock` and `ss -ltnp` shows no TCP listener held on the new service's behalf. Recorded in verify.md §11.

## 9. Documentation

- [x] 9.1 Record the two-service topology, the dedicated forge application, and the boundaries that keep the new service from building anything, in the repository's documentation tree — verify: `just docs-build` succeeds and the documentation link check passes

## 10. Integration Verification

- [x] 10.1 Verify the new service answers at its own hostname over an accepted connection — verify: an HTTPS request to `https://nixbot.scientistexperience.net/` returns a successful response with a certificate issued for that hostname. `curl` returns HTTP 200 with `ssl_verify_result=0` in 0.38s. The certificate is Let's Encrypt `CN=nixbot.scientistexperience.net`, notBefore `Aug 28 01:01:58 2026 GMT`, notAfter `Nov 26 01:01:57 2026 GMT`, issued by the service's own `acme-nixbot.scientistexperience.net.service` with its own renewal timer. Recorded in verify.md §10.1.
- [x] 10.2 Verify the incumbent still serves — verify: an HTTPS request to `https://buildbot.scientistexperience.net/` returns a successful response, and the incumbent's units on the host report active. The request completes over a valid chain (`ssl_verify_result=0`) and returns HTTP 403, which is settled as this service's configured anonymous response rather than a regression, on four independent grounds detailed in verify.md §10.2: the 403 is emitted by oauth2-proxy on `127.0.0.1:8020`, continuously active since `2026-08-14 23:12:13 UTC` with an unchanged MainPID 1771, thirteen days before this deployment; the body is oauth2-proxy's own 8486-byte `<title>Sign In</title>` page; buildbot's own access log and every rotation of it contain zero 403 responses ever and never saw a distinctively-tagged probe, so unauthenticated requests have never reached buildbot before or after the change; and the incumbent is deliberately authenticated by `modules/nixos/buildbot.nix:118-125` via upstream `authBackend = "httpbasicauth"`, with `allow_unauthenticated_control = false`. `buildbot-master.service` and `buildbot-worker.service` both report `active running`, the incumbent accepted a forge delivery at 02:05:08, and it started five builds at 02:08:03-04, after nixbot came up at 02:01. Amended only in that the task text presumed a 2xx response; the observable requirement — the incumbent still serves — is met, and the status code is explained rather than assumed.
- [x] 10.3 Verify the new service builds nothing — verify: its interface reports an empty repository set, and no build has been recorded since deployment. The operator observed the user-facing interface reporting no repositories set up to build; the same surface rendered anonymously reads `Repositories no repositories enabled yet`. The reporting path is the HTML interface at `/`, recorded because it differs from the path `plan.md:570` anticipated: `/api/v1/projects`, `/api/projects` and `/projects` all return 404 at the pinned revision. In the service's own state `projects`, `builds`, `check_runs`, `build_attributes` and `failed_builds` all have zero rows, and the evaluated configuration sets both boundaries, `github.topic = null` and `github.repoAllowlist = [ ]`. Strongest evidence is a live negative test rather than an absence: a genuine push to the installed repository (`4fb2eeef`, repo id 807284872) was received, enqueued and processed to `done` in 12 ms with no error and created zero builds. Recorded in verify.md §10.3.
- [ ] 10.4 Verify delivery authentication both ways — verify: a redelivery from the forge application's advanced settings is accepted with a successful response, and a request to the same endpoint without the shared secret is rejected. UNCHECKED: the redelivery is available only from the forge application's advanced settings tab under the operator's own identity and was not attempted. The accepted arm is incidentally established — two deliveries returned `202 Accepted` and `200 OK`, and the first is the enqueued-and-dropped event in 10.3 — but an endpoint that accepted everything would look identical, so the rejection arm is what distinguishes them and it has not been run. Exact operator steps and the exact expected evidence for both arms are written out in verify.md §10.4; tick this only when both are observed.
- [x] 10.5 Verify both databases exist on the one instance — verify: listing databases on the host shows the incumbent's and the new service's side by side, each owned by its own role. `psql -c '\l'` lists `buildbot` owned by role `buildbot` and `nixbot` owned by role `nixbot`, side by side on the one instance; `psql -c '\du'` shows neither role carrying superuser or createdb. The new database was created by the module's own `database.createLocally = true` (`modules/nixos/nixbot.nix:115`) and its schema is fully migrated, 24 migrations applied at startup from `initial` through `drop_forge_tokens`. Recorded in verify.md §10.5.
- [x] 10.6 Verify no verdict namespace collision — verify: no check run bearing the incumbent's prefix originates from the new service, and no repository's required checks name the new service's prefix. First arm: `nix eval --raw .#nixosConfigurations.magnetite.config.services.nixbot.statusContextPrefix` returns `nixbot`, the module default, and no assignment to that option exists anywhere in the repository; the incumbent's contexts are `buildbot/...`. Stronger, the new service's `check_runs` table has zero rows, so it has emitted no check run at all under any prefix. Second arm: the one installed repository's default branch is governed by ruleset `16212553`, whose sole required context is `buildbot/nix-build` pinned to `integration_id 3305657` — `sciexp-buildbot`, not the new application `4743700` — and classic branch protection on `main` declares no required status checks. Recorded in verify.md §10.6.
- [x] 10.7 Verify the capacity picture after deployment — verify: memory in use, the store dataset's free space against its quota, and the incumbent's evaluation and build activity are recorded in verify.md as the baseline the first repository opt-in will be measured against. Recorded in verify.md §10.7: 12 GiB of 30 GiB in use with 17 GiB available and 298 MiB of 30 GiB swap; the store dataset `zroot/root/nix` at 48.7 G of its 250 G quota, 20 percent, with 201 G free; the nixbot unit as a single uvicorn process on a unix socket with no worker pool spawned, against a configured ceiling of `evalWorkerCount = 2` at `evalMaxMemorySize = 2048`; and the incumbent's activity as 59,164 builds retained under a four-week janitor horizon (54,909 success, 1,683 failure, 1,921 exception, 636 cancelled, 7 warnings, 8 in flight), highest id 60436, with five builds started inside one second at 02:08:03-04. The section states that the 12 GiB figure includes the incumbent's active builds and none of nixbot's eventual eval load, so the first opt-in is measured as a delta against it.
- [x] 10.8 Verify the rollback path is available — verify: removing the aspect from the host's list builds cleanly as a check, establishing that withdrawal is a redeploy rather than a repair. Amended from the realizing `nix build` of `plan.md:598` to instantiation, for the reason task 7.1 already records: the operator's machine is aarch64-darwin against an x86_64-linux target, so realizing copies an entire system closure locally for information instantiation already supplies. Deleting the `nixbot` entry from magnetite's aspect list and instantiating gave `nix eval .#checks.x86_64-linux.nixos-magnetite.drvPath` = `/nix/store/28ar1w6mdn20kf7qf3v3bvq89q3c9cv3-nixos-system-magnetite-...drv`, exit 0, against `/nix/store/wshfaafhcyalpsc25czjlnsci0dy43jz-nixos-system-magnetite-...drv` with the aspect present on the same tree — two distinct derivations, both instantiating with no evaluation error, option collision or assertion failure. The edit was reverted. No realization of either toplevel was performed. Recorded in verify.md §10.8.
