## 1. Pin the upstream Mergify skill source

- [x] 1.1 Verify through `gh-axi api` that tag ref `2026.8.31.1` resolves to `727ce50b8fb3be8a9a24025807e159d644dbba80` and that the recursive tree contains only `skills/mergify-stack/SKILL.md` beneath the skill directory — verify: `gh-axi api repos/Mergifyio/mergify-cli/git/ref/tags/2026.8.31.1 --jq '.object.sha'` prints that revision, and `gh-axi api 'repos/Mergifyio/mergify-cli/git/trees/727ce50b8fb3be8a9a24025807e159d644dbba80?recursive=1' --jq '.tree[] | select(.path | startswith("skills/mergify-stack/")) | .path'` prints only the entry point
- [x] 1.2 Add `pkgs/by-name/agent-plugins/mergify-cli/package.nix` as a source-only `fetchFromGitHub` package carrying release `2026.8.31.1`, revision `727ce50b8fb3be8a9a24025807e159d644dbba80`, and hash `sha256-BQl5L61m6uSr7y7fXoUsEwELmmSmHvs0jJMH22Zm82A=`, commit that file alone, and retain `pkgs/by-name/mergify-cli-bin/package.nix` unchanged — verify: `nix eval --option builders '' --option allow-import-from-derivation false --raw .#packages.aarch64-darwin.agent-plugins-mergify-cli.rev`, `nix eval --option builders '' --option allow-import-from-derivation false --raw .#packages.aarch64-darwin.mergify-cli-bin.version`, and `git diff-tree --no-commit-id --name-only -r HEAD` show the aligned source identity, `2026.8.31.1`, and only the source package path
- [x] 1.3 Build the source package before beginning composition work, with rollback defined as reverting its one-file commit — verify: `nix build --option builders '' .#agent-plugins-mergify-cli --no-link --print-out-paths` exits 0 and the output contains `skills/mergify-stack/SKILL.md`

## 2. Compose and register the upstream skill

- [x] 2.1 Add the full-revision `Mergifyio/mergify-cli` skill-subset dependency to `modules/home/ai/plugins/version-control-and-forge/apm.yml`, commit that file alone, and leave `apm.lock.yaml` unedited — verify: `rg -n -A4 -B2 'Mergifyio/mergify-cli|mergify-stack' modules/home/ai/plugins/version-control-and-forge/apm.yml` shows one remote dependency and `git diff-tree --no-commit-id --name-only -r HEAD` shows only the package manifest
- [x] 2.2 Add the Mergify source argument, declaration drift guard, delivered `SKILL.md` entry-point assertions, and one checkout-cache pre-warm whose shard hashes APM 0.29.0's normalized lowercase URL `https://github.com/mergifyio/mergify-cli` while the manifest retains `Mergifyio/mergify-cli`, then commit that file alone — verify: `rg -n "SHARD_MF=.*https://github.com/mergifyio/mergify-cli" pkgs/by-name/apm-skills-compose/package.nix` finds the sole Mergify shard, `nix build --option builders '' .#apm-skills-compose --no-link --print-out-paths` resolves offline through that shard and exits 0 in the Nix sandbox, the resulting generated lock records the resolved remote revision and per-file hash, and the output contains `mergify-stack/SKILL.md` under `.claude/skills` and `.agents/skills`
- [x] 2.3 Register the upstream skill and its first-party owner in `modules/home/ai/plugins/README.md`, then commit that file alone — verify: `rg -n 'mergify-stack|git-stacked-pr-integration' modules/home/ai/plugins/README.md` names both skills, their distinct ownership, and the `version-control-and-forge` package
- [x] 2.4 Confirm the composition step is independently shippable, with rollback defined as reverting the README, compose, and manifest commits in reverse order — verify: a fresh `nix build --option builders '' .#apm-skills-compose --no-link --print-out-paths` exits 0 without modifying tracked files, and `git status --short` is empty

## 3. Converge first-party policy on the upstream mechanism

- [x] 3.1 Revise `modules/home/ai/plugins/version-control-and-forge/.apm/skills/git-stacked-pr-integration/SKILL.md` to state worker and orchestrator contracts, refer to `mergify-stack` for Mergify command mechanics, and name the landed repository `stack-land --tip REV PR...` command as the checked landing handler without inventing an upstream landing recipe — verify: `rg -n 'worker|orchestrator|mergify-stack|stack-land --tip' modules/home/ai/plugins/version-control-and-forge/.apm/skills/git-stacked-pr-integration/SKILL.md` finds every contract and pointer
- [x] 3.2 Preserve the PR 2738, 2739, and 2740 evidence sentence from base and add the soft note as the literal transcript-prescribed text “we start with git… The `Change-Id` format is shared, so switching the orchestrator or individual workers to jj changes nothing about identity, PR bookkeeping, or landing. The signal to switch is conflict volume in the orchestrator step, not preference.”, commit the skill file alone, and define rollback as reverting that commit — verify: the edited source contains the base evidence sentence byte-for-byte, `rg -Fxc 'we start with git… The `Change-Id` format is shared, so switching the orchestrator or individual workers to jj changes nothing about identity, PR bookkeeping, or landing. The signal to switch is conflict volume in the orchestrator step, not preference.' modules/home/ai/plugins/version-control-and-forge/.apm/skills/git-stacked-pr-integration/SKILL.md` prints `1`, and `git diff-tree --no-commit-id --name-only -r HEAD` shows only the skill path
- [x] 3.3 Verify the changed source reaches the composed corpus rather than stopping at the authored file — verify: a fresh `nix build --option builders '' .#apm-skills-compose --no-link --print-out-paths` exits 0 and `rg -n 'mergify-stack|stack-land --tip' <result>/.claude/skills/git-stacked-pr-integration/SKILL.md` finds the delivered pointers

## 4. Add the role-conditioned user-context protocol

- [x] 4.1 Add a short `Stacked landing protocol` subsection under `Version control and work dispatch` in `modules/home/tools/agents-md.nix` that names the user-context tier; keeps an OpenSpec change normally bound to one Linear story as the dispatch unit; encodes each independently shippable worker step as one delivery commit and one pull request; requires corrections to update that same commit without rewriting unrelated history outside the stack unit; reserves assembly and landing for the orchestrator; and defers mechanics to the two skills plus `stack-land` — verify: `rg -n -A20 -B3 'Stacked landing protocol' modules/home/tools/agents-md.nix` shows the role boundary and no copied landing recipe
- [x] 4.2 Make the subsection state that VCS detection selects local authoring and working-copy mechanics without changing stack identity, pull-request bookkeeping, or landing; keep the jj working-copy, hazard, recovery, and worktree-interop paragraphs authoritative for jj local operations; preserve the literal soft note “we start with git… The `Change-Id` format is shared, so switching the orchestrator or individual workers to jj changes nothing about identity, PR bookkeeping, or landing. The signal to switch is conflict volume in the orchestrator step, not preference.”; and forbid project context from becoming a superset of the user-level context — verify: `rg -n 'Dispatch unit and version control mode|Commit behavior override|Making changes in jj-managed|Working-copy hazards|Worktree interop|containment de-duplication|Stacked landing protocol' modules/home/tools/agents-md.nix` finds every governing section, and `nix eval --option builders '' --option allow-import-from-derivation false --raw .#darwinConfigurations.stibnite.config.home-manager.users.crs58.programs.agents-md.settings.text | rg -Fxc 'we start with git… The `Change-Id` format is shared, so switching the orchestrator or individual workers to jj changes nothing about identity, PR bookkeeping, or landing. The signal to switch is conflict volume in the orchestrator step, not preference.'` prints `1`
- [x] 4.3 Evaluate the generated context instead of treating the Nix source as delivery evidence — verify: `nix eval --option builders '' --option allow-import-from-derivation false --raw .#darwinConfigurations.stibnite.config.home-manager.users.crs58.programs.agents-md.settings.text | rg -n -A20 -B3 'Stacked landing protocol'` emits the role-conditioned subsection with its explicit precedence, skill, and `stack-land` pointers
- [x] 4.4 Commit the generator file alone and define rollback as reverting that commit — verify: `git diff-tree --no-commit-id --name-only -r HEAD` shows only `modules/home/tools/agents-md.nix`

## 5. Integration Verification

- [x] 5.1 Validate the change artifacts and record the canonical direct-target conflict as an undischarged provenance risk rather than a repaired requirement — verify: `openspec validate integrate-mergify-stacked-landing --strict` exits 0, and `rg -n 'undischarged provenance risk|agent-skills.*claude.*codex.*hermes|fan.*out' openspec/changes/integrate-mergify-stacked-landing openspec/specs/first-party-skill-distribution/spec.md` shows both the recorded conflict and the unchanged canonical text
- [x] 5.2 Verify the source pin, Nix-output-generated lock, composed corpus, first-party ownership text, and rendered user context together without activation or a real landing — verify: the targeted evaluations and builds from tasks 1.2, 2.2, 3.3, and 4.3 all exit 0 in one clean revision, both skill names resolve, and `git status --short` is empty
- [x] 5.3 Confirm the implementation stayed within CAM-41's exclusions — verify: the recorded integrated-main commit `47ca9d8dec2cf10a985e8e6587e10a87c3df741b` is the immutable feature-scope boundary after the deliberate `origin/main` integration; `git diff --name-only 47ca9d8dec2cf10a985e8e6587e10a87c3df741b..HEAD` contains no Cognee path, `pkgs/by-name/mergify-cli-bin/`, home-manager Mergify installation, `pkgs/by-name/stack-land/`, `modules/home/tools/stack-land.nix`, nixbot or ruleset setting, or manually edited `apm.lock.yaml`; and the two `pkgs/by-name/stack-land/` paths belong to integrated-main provenance rather than CAM-41 feature edits

## 6. Reconcile accepted review findings before reverification

- [x] 6.1 Correct `git-stacked-pr-integration` so a worker prepares and verifies one change and returns its ref and evidence without publishing, while the orchestrator alone orders and publishes the pull-request stack and alone lands it; match the merged `stack-land` reported-check predicate by accepting only nonempty sets of `SUCCESS`, `NEUTRAL`, or `SKIPPED` conclusions and blocking pending, failing, or any other conclusion; keep the PR 2738, 2739, and 2740 evidence sentence byte-identical while adding adjacent prose that limits it to the observed fast-forward landing and GitHub reachability rather than Mergify authoring or publication; then commit the skill file alone — verify: the `Role contracts`, `Requirement-to-mechanism map`, and `Checked landing boundary` sections contain those exact ownership, state, and evidence boundaries, the protected evidence sentence still has one exact match, and `git diff-tree --no-commit-id --name-only -r HEAD` shows only the skill path
- [x] 6.2 Correct the generated user-context protocol so the worker returns its delivery commit and evidence without publishing and the orchestrator alone orders refs, publishes the pull-request stack, and lands it, then commit the generator alone — verify: evaluation of `.#darwinConfigurations.stibnite.config.home-manager.users.crs58.programs.agents-md.settings.text` with builders disabled and import-from-derivation forbidden shows that ownership in `Stacked landing protocol`, preserves the exact soft routing note, and `git diff-tree --no-commit-id --name-only -r HEAD` shows only `modules/home/tools/agents-md.nix`
- [x] 6.3 Add the auto-discovered `modules/checks/structure/mergify-release-alignment.nix` module and leave `apm-skills-compose` unchanged from Task 2; read `self'.packages.agent-plugins-mergify-cli.version` and `self'.packages.mergify-cli-bin.version` into a reusable `runCommand` checker that fails with both names and values when they differ; register `checks.<system>.structure-mergify-release-alignment` for those evaluated values and `checks.<system>.structure-mergify-release-alignment-neg` as a genuine failing-case control that invokes the same checker with unequal fixtures, requires failure, and verifies both fixture values in the diagnostic; then commit the check file alone — verify: targeted builds of both check attributes with builders disabled exit 0, the negative control would fail if the shared checker accepted its unequal fixtures or omitted either value, both named package-version evaluations return `2026.8.31.1`, and `git diff-tree --no-commit-id --name-only -r HEAD` shows only the new structure-check module
- [x] 6.4 Preserve the root-lock delivery boundary and record the post-merge follow-up without editing the lock — verify: `git diff --quiet 33f94e2ce..HEAD -- apm.lock.yaml` exits 0, the root lock contains neither `Mergifyio/mergify-cli` nor `mergify-stack`, and the change artifacts state that a fresh frozen `just agents-install` before relock would materialize the pre-change set while the contents of any existing ignored repository-local `.agents/` tree are unspecified; after this change reaches `main`, a mandatory follow-up runs `just agents-relock`, reviews and commits the generated root lock, and only then claims frozen repository-local delivery
- [x] 6.5 Correct the ignored `.superpowers/sdd/task-2-report.md` conclusion that says GREEN remains blocked after its own resolution section, without creating a tracked commit — verify: `git check-ignore -q .superpowers/sdd/task-2-report.md` exits 0, its final status agrees with the recorded successful normalized-shard build, and `git status --short` does not list the report
- [x] 6.6 Re-run strict OpenSpec validation and the focused release-alignment, composition, policy, and rendered-context checks, then hand the evidence to the subsequent post-Task-6 verifier — verify: `openspec validate integrate-mergify-stacked-landing --strict` exits 0, the focused commands in tasks 6.1 through 6.4 pass, and the evidence handoff is complete
  Task 6 completion records that handoff, not report regeneration.
  The subsequent verifier MUST regenerate `verify.md` with citations to named check attributes, package attributes, Markdown sections, and commands rather than line numbers before final acceptance.
