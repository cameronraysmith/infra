# Tasks

Each task names its verification.
Stage numbers S0–S2 match proposal.md and ADR-007; tasks within a stage depend on the preceding tasks of that stage unless stated, and each stage depends on the one before it.
Stages S3, S4, S4b, and S5 are `openspec/changes/capi-hetzner-cluster/tasks.md`; its S3 depends on this change's S2.
Stage D is this change.
Ambiguities A4, A8, A10–A12 (design.md) gate the tasks that name them.
No task in this change spends money or touches a cloud; no task edits `kubernetes/clusters/local*`, `kubernetes/nixidy/`, `kubernetes/tests/local-k3d/`, or `modules/apps/cluster/`, and every task's verification includes a `git diff --stat` check that those paths are absent.

## D. Design (this change)

- [x] D.1 Record research findings, the assertion-to-tier table, and the option analysis in ADR-007; record the reconciler and artifact decisions in ADR-008; record the node-management, multi-cloud, and networking decisions in ADR-009 — verify: the three files exist under `docs/notes/development/kubernetes/decisions/`, are indexed in `docs/notes/development/kubernetes/README.md`, every path:line citation resolves in the reference trees at the revisions ADR-007's appendix lists, and each ADR's provenance table maps every design-review decision code to an ADR decision
- [x] D.2 Write this change's brainstorm, proposal, design, delta specs, and tasks — verify: `direnv exec . openspec validate k3s-nixos-vm-tests` passes, and each capability named in proposal.md has a `specs/<capability>/spec.md` whose requirements each name a coverage bin and non-vacuity evidence
- [x] D.3 Revision 2: narrow ADR-007 and ADR-008 to `<c>` with D1–D31 statuses, revise ADR-009, add ADR-010, record the target module layout in design.md, and split S3–S5 into `openspec/changes/capi-hetzner-cluster/` — verify: ADR-010 is indexed in the README; every decision of revision 1 appears in design.md as kept, narrowed, moved, or superseded; `direnv exec . openspec validate --all` passes for both changes; `git diff --stat` touches nothing under the frozen paths
- [ ] D.4 Obtain answers to A4, A8, A10–A12 — verify: the answers are recorded in design.md Open Questions and the gated tasks below are unblocked or rewritten; A10 (the name of `<c>`) is applied as a search-and-replace across both changes and ADR-007–ADR-010

## S0. Purity, provenance, and identity regulators (KVM-free)

- [ ] S0.1 Add `modules/nixos/k3s-server/snapshotter.nix` with the `k3s-server.snapshotter` option (default `"nix"`, emits `--snapshotter <value>`, puts `pkgs.nix` on the unit path), import it from `default.nix`, and delete the inert `virtualisation.containerd.settings` block — verify: `nix eval` of the standalone module shows `--snapshotter nix` in `ExecStart` and no `virtualisation.containerd` attribute; `git diff --stat` touches only `modules/nixos/k3s-server/`
- [ ] S0.2 Add `modules/checks/k3s-server-eval.nix` (T1) asserting the evaluated module's `ExecStart` flags including `--snapshotter nix`, kernel modules, sysctls, firewall lists, unit path, and containerd shape — verify: `nix build .#checks.x86_64-linux.k3s-server-eval` passes on a host without KVM; removing `--disable-kube-proxy` from the module makes it fail naming the flag; setting `snapshotter = "overlayfs"` makes it fail naming `--snapshotter nix`; revert both
- [ ] S0.3 Add `kubernetes/clusters/<c>/{default.nix,topology.nix}` and `kubernetes/modules/flux/` as the easykubenix cluster module of `<c>` with the cloud-invariant core (Cilium with `kubeProxyReplacement=true`, `CiliumLoadBalancerIPPool`, Flux root objects, cert-manager, step-ca, `ClusterIssuer`, `Gateway`, `Certificate`s, `HTTPRoute`), third-party images vendored by digest, and a `target` selecting production or VM fixtures; add `<c>` to `evalCluster` in `modules/kubernetes.nix` — verify: `nix build .#k8s-manifests-<c>` renders; `nix eval .#checks.x86_64-linux --apply builtins.attrNames` still lists `nixidy-k8s`; `rg -i 'argocd|sops-secrets-operator' kubernetes/clusters/<c> kubernetes/modules/flux` returns nothing; `git diff --stat` shows `modules/kubernetes.nix` gaining lines only
- [ ] S0.4 Add `modules/kubernetes/{artifacts,oci-lib}.nix`: `k8s-manifests-<c>`, the OCI-layout derivation `k8s-oci-<c>` (`oci-layout`, `index.json`, `blobs/sha256/`, manifest digest in `$out/digest`), and `mkOciLayout`, `mkNixSnapshotterImage`, `readDigest` — verify: `nix build --rebuild .#k8s-oci-<c>` yields byte-identical output; `crane manifest` over the layout reports the digest in `$out/digest`
- [ ] S0.5 Add `modules/kubernetes/purity.nix` → `checks.k8s-purity-<c>` (`flakeRef`, `nixExpr`, `:latest`, untagged, tag-without-digest; rendered images ⊆ preload set derived by string context) — verify: passes; reintroducing `alpine/curl:latest` in `<c>` makes it fail naming the reference; adding a `Deployment` with an unpreloaded image makes it fail naming the image; revert both
- [ ] S0.6 Add `modules/kubernetes/provenance.nix` → `checks.k8s-provenance-<c>` (`nix path-info -r` of the `<c>-server` closure, image digest inventory with producing derivation, OCI-layout digest) — verify: `nix build --rebuild` is byte-identical; bumping one image digest changes exactly one line
- [ ] S0.7 Add `modules/kubernetes/identity.nix` → `checks.k8s-node-identity-free-<c>` over the rendered Flux artifact, `secrets/clusters/<c>/`'s sops index, and the Clan inventory (the CAPI CRs are added to its inputs by the sibling change's S3) — verify: passes; adding a Secret named after a hostname pattern `<c>-server-0` or a `hosts.<c>-agent` entry to the inventory fails it naming the reference; revert
- [ ] S0.8 Add `modules/kubernetes/flux/install.nix` (`flux install --export --components=source-controller,kustomize-controller,notification-controller` from `pkgs.fluxcd`) and `flux/root.nix` (root `OCIRepository` with `spec.ref.digest` from S0.4 and `spec.verify`, root `Kustomization` with `decryption.provider: sops`), and the T1 leaves `flux-install-rendered` and `flux-sources-pinned` — verify: both pass; adding `helm-controller` to the components fails `flux-install-rendered`; rendering the root `OCIRepository` with `spec.ref.tag` only fails `flux-sources-pinned`; revert both
- [ ] S0.9 Add `modules/apps/k8s/oci-push` (E) that pushes the layout to GHCR under the flake-revision tag, reads back the registry digest, and exits non-zero on inequality, and `modules/apps/k8s/cosign-sign` (E) that signs the pushed digest with the `<c>` cosign key; run neither against GHCR in this stage — verify: a dry run against a local `docker-distribution` on the developer host prints equal digests and exits zero; pushing a different layout under the same tag exits non-zero naming both digests; `nix path-info -r` over the checks closure contains neither effect
- [ ] S0.10 Add `secrets/clusters/<c>/` with Clan vars generators for the T0 set (k3s token, k3s CA set, Flux age key, cosign key, SSH CA, etcd-S3 credentials; ADR-010 D10.1) — verify: `clan vars generate` produces the files; `k8s-node-identity-free-<c>` passes; `git diff --stat` shows no other `secrets/` path
- [ ] S0.11 Add flux, cosign, and crane to `modules/devshells/kubernetes.nix` — verify: `direnv exec . which flux cosign crane` resolves; `git diff --stat` shows the file gaining lines only

## S1. Substrate and snapshotter leaves (KVM)

- [ ] S1.1 Add `modules/checks/vm-k3s-substrate.nix` following `vm-nixos-base.nix`: one machine importing `flake.modules.nixos.base` and `k3s-server` with `enable`, `clusterInit`, store-path `tokenFile`, `memorySize = 1536`, `diskSize = 4096`, no CNI — verify: `nix build .#checks.x86_64-linux.vm-k3s-substrate 2>&1 | tee logs/vm-k3s-substrate-$(date +%Y%m%d-%H%M%S).log` passes on the KVM host; wall time recorded in the PR body
- [ ] S1.2 Write the single-node test script asserting the T2 rows of ADR-007 Q1 plus the snapshotter rows: service active, node registered, `Ready=False` with `reason == KubeletNotReady` and message containing `cni plugin not initialized`, CoreDNS `Pending`, no flannel/cni0 link, no `cni-` netns, no kube-proxy process or `KUBE-SERVICES` chain, `br_netfilter`/`nf_conntrack`/`overlay` loaded, sysctl values, TCP 6443 accepted and an unlisted port dropped, kubeconfig unreadable by `noprivs`, `k3s-killall.sh` clean, `k3s ctr plugins ls` lists `io.containerd.snapshotter.v1 nix ok`, the generated containerd config names `nix` as CRI snapshotter, NRI plugin state logged as an observation — verify: the leaf passes; each assertion's stdout appears in the test log; the NRI observation is quoted in the PR body
- [ ] S1.3 Record mutation evidence for the single-node leaf: set `net.ipv4.ip_forward = 0`, rebuild, capture; remove `6443` from the firewall, rebuild, capture; set `snapshotter = "overlayfs"`, rebuild, capture the plugin assertion failure; revert all — verify: the three failure outputs and the revert are in the PR body and `git diff main -- modules/nixos/k3s-server/` is empty
- [ ] S1.4 Add `modules/checks/vm-k3s-snapshotter.nix`: single node as S1.1 with a test-only flannel override as CNI glue, one pod whose image is `nix-snapshotter.buildImage` (through `mkNixSnapshotterImage`) referenced `nix:0<store path>` — verify: the pod reaches `Running` and `kubectl exec` shows the store-path binary; removing `pkgs.nix` from the unit path makes the pod fail with an error naming `nix-store`; revert
- [ ] S1.5 If S1.2 observed NRI disabled, set `services.k3s.containerdConfigTemplate` in the module to enable it and extend S1.2 to assert `[plugins."io.containerd.nri.v1.nri"] disable = false`; otherwise record that the default template suffices — verify: the module diff or the recorded observation is in the PR body
- [ ] S1.6 Confirm the buildbot worker's behavior on the `kvm`-requiring leaves — verify: buildbot's evaluation of the S1 branch reports every pre-existing check with an unchanged verdict and each VM leaf as absent or skipped, per `ci-log-verification`; if it reports a failure, add the `requiredSystemFeatures` filter to the buildbot-nix configuration in the same stage

## S2. Platform leaf: the `<c>` core through Flux from an offline registry (KVM)

- [ ] S2.1 Add `kubernetes/tests/<c>/` with a Chainsaw suite authored for `<c>` (A11): foundation steps (Cilium `DaemonSet` ready, `CiliumLoadBalancerIPPool` present, Flux controllers ready, root `Kustomization` `Ready=True` with `lastAppliedRevision` ending in the S0.4 digest) and infrastructure steps (cert-manager, step-ca, `ClusterIssuer` `Ready`, `Gateway/main-gateway` `Programmed=True` with each listener `Accepted=True`, every declared `Certificate` `Ready=True`, `HTTPRoute` `Accepted=True`); no ArgoCD or sops-secrets-operator step — verify: `chainsaw lint` passes; `rg -il 'argocd|sopssecret' kubernetes/tests/<c>` returns nothing; `git diff --stat` shows nothing under `kubernetes/tests/local-k3d/`
- [ ] S2.2 Add the committed test-only age keypair and cosign keypair under `kubernetes/tests/fixtures/` with a README stating they are test fixtures standing in for the `<c>` T0 keys and listing what they do not cover — verify: `gitleaks` in `prek run --all-files` passes with the fixtures allow-listed by path; the README lists Clan vars generation, recipients, rotation, and `contentFrom.secret` delivery as uncovered
- [ ] S2.3 Add nix2container or fixed-output pull derivations for every image in the rendered `<c>` tree plus pause and CoreDNS, and measure — verify: `nix path-info -S` on the set is in the PR body and compared against the 1.5–2.5 GiB bound in ADR-007 F4; `k8s-purity-<c>` passes
- [ ] S2.4 Add `modules/nixos/k3s-flux.nix` (Flux install and root objects from S0.8 into `services.k3s.manifests`, digest pinned from S0.4) and `modules/machines/nixos/<c>-server.nix`, `<c>-agent.nix` (image-only, role fixed at build, both importing `base` and `k3s-server`) — verify: `nix eval` of `<c>-server` shows the manifests and the digest; `nix eval` of `<c>-agent` shows `--server=` and neither CIDR flag; `k8s-node-identity-free-<c>` passes with both machines in scope
- [ ] S2.4b Add `checks.k8s-closure-secret-free-<c>` to `modules/kubernetes/identity.nix` walking the `<c>-server` and `<c>-agent` closures for sops files from `secrets/clusters/<c>/`, T0 private-key fingerprints, and the S2.2 fixture private keys (ADR-010 R10.2) — verify: passes; adding the fixture age private key to `environment.etc` of `<c>-server` makes it fail naming the store path; revert
- [ ] S2.5 Add `modules/checks/vm-k3s-platform.nix`: `<c>-server` and `<c>-agent` on one VLAN with `services.k3s.nodeIP` as glue; `services.k3s.images` from S2.3 on both; Cilium with `kubeProxyReplacement=true` and the `CiliumLoadBalancerIPPool`, the `sops-age` and cosign public-key Secrets from S2.2, and the root `OCIRepository`/`Kustomization` through `services.k3s.manifests` on the server; a `docker-distribution` registry in the server guest (A4) seeded at activation by `crane push` from the layout; CoreDNS answering the certificate hostnames; `pkgs.chainsaw` in the server guest; sizing starts at `memorySize = 6144; diskSize = 16384` for the server and `4096; 8192` for the agent — verify: `nix build .#checks.x86_64-linux.vm-k3s-platform 2>&1 | tee logs/vm-k3s-platform-$(date +%Y%m%d-%H%M%S).log` passes on the KVM host; wall time recorded
- [ ] S2.6 Write the platform test script: wait for `k3s.service` on both guests, assert both nodes registered and the agent reaches `server:6443` and `server:10250`, assert `kubectl -n kube-system get cm cilium-config -o jsonpath='{.data.kube-proxy-replacement}'` is `true` and no `kube-proxy` process exists on either guest, schedule one pod per node and assert pod-to-pod connectivity across guests, then run `KUBECONFIG=/etc/rancher/k3s/k3s.yaml chainsaw test <store path of kubernetes/tests/<c>>` and copy the report out — verify: Chainsaw exits zero; the report is in the test output; no `k3d-wait-*` logic is ported
- [ ] S2.7 Record mutation evidence: swap `spec.ref.digest` for a different layout's digest, capture the `OCIRepository` fetch failure; sign with a foreign cosign key, capture `SourceVerified=False`; edit the Gateway assert to one listener more than declared, capture the Chainsaw failure; remove the `CiliumLoadBalancerIPPool`, capture `AddressNotAssigned`; delete the `sops-age` Secret, capture the decryption failure; remove `6443` from the firewall, capture the agent never registering; revert all — verify: the six failures are in the PR body
- [ ] S2.8 If S2.5's wall time exceeds fifteen minutes, split `vm-k3s-cilium` out per design D6 — verify: the split change records the measured time and each leaf passes independently

## Execution wiring (after S1; the `integration` job is not edited)

- [ ] X.1 Add a `workflow_dispatch`-only probe job to `.github/workflows/test-cluster.yaml` on `ubuntu-latest` that builds `vm-k3s-substrate` with `--option system-features 'kvm nixos-test benchmark big-parallel'` — verify: three consecutive dispatches pass with run links recorded, or the failure is recorded and VM leaves remain developer-host regulators; `git diff` of the workflow shows the `integration` job unchanged
- [ ] X.2 If X.1 passed, add a `vm` job building every `checks.x86_64-linux.vm-*` leaf through `cached-ci-job` with `hash-sources` covering `modules/checks/vm-*.nix modules/nixos/** modules/kubernetes/** kubernetes/clusters/<c>/** kubernetes/modules/** kubernetes/tests/<c>/** kubernetes/tests/fixtures/**` — verify: the job passes; a sysctl change in `kernel.nix` produces a cache miss; the `integration` job still runs
- [ ] X.3 Update `docs/notes/development/kubernetes/` (README, `workflows/06-testing.md`, `components/nixos-k3s-server.md`) to name every `vm-*` and `k8s-*` leaf that exists and to state that `local-k3d` keeps its own workflow — verify: `prek run --all-files` passes; ADR-006 is not marked superseded

## Integration Verification

- [ ] IV.1 On the KVM developer host, `direnv exec . just test-integration 2>&1 | tee logs/test-integration-$(date +%Y%m%d-%H%M%S).log` builds every `vm-*` leaf from a clean store and each passes — verify: the log ends with each leaf's store path
- [ ] IV.2 `nix eval .#checks.aarch64-darwin --apply builtins.attrNames` lists no `vm-*` leaf, and `nix eval .#checks.x86_64-linux --apply builtins.attrNames` lists every T1 leaf named in S0 and still lists `nixidy-k8s` — verify: command output
- [ ] IV.3 The buildbot worker's evaluation of the final branch reports every non-VM check with an unchanged verdict, every S0 T1 leaf passing, and no VM leaf as a failure — verify: `ci-log-verification` on the buildbot run
- [ ] IV.4 No `checks.<system>.*` derivation references an `apps` publishing script — verify: `nix path-info -r` over the checks closure contains none of the `modules/apps/k8s/*` store paths
- [ ] IV.5 The mutation evidence for S0–S2 is present in the merged PR bodies and each mutated file is byte-identical to `main` after revert — verify: PR body links and `git diff main -- modules/nixos/k3s-server/ kubernetes/tests/<c>/` empty
- [ ] IV.6 The frozen prototypes are untouched by S0–S2 — verify: `git diff main --stat -- kubernetes/clusters/local kubernetes/clusters/local-k3d kubernetes/nixidy kubernetes/tests/local-k3d modules/apps/cluster modules/nixidy.nix .github/workflows/test-cluster.yaml` shows only the X.1/X.2 job additions in the workflow and nothing else
- [ ] IV.7 Every requirement in the five delta specs is discharged by a named leaf, effect, or review rule with a coverage bin and non-vacuity evidence, and every world assumption is cited by the requirements that rest on it — verify: `direnv exec . openspec validate k3s-nixos-vm-tests` passes and a traceability pass over the specs finds no undischarged requirement
